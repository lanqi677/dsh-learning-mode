# 学习模式插件 · 换机器继续开发说明

更新时间：2026-09-18　｜　当前机器：Linux，DSH_HOME=`/home/<user>/.dsh`，profile=`web`
上一版来源：Windows，DSH_HOME=`C:\Users\<user>\.dsh`（历史踩坑保留在 §4）

---

## 1. 这是什么

给 DSH 加一个「学习模式」：一棵由 AI 维护、可无限下钻的学习树，节点可标完成、可聚焦，
每个节点配一句**自动生成的摘要**，每轮对话自动带上"我在哪 + 这层摘要 + 还没学完什么"，
另有**复习视图**一处看完所有已完成知识点。

解决的具体痛点：**学习时钻进支线，回头找不到原来的位置**（复习链表时去看跳表，看完回不来；
要复习十个知识点，每学完一个都得翻聊天记录找下一个）。

---

## 2. 当前状态（本机实测，不是推断）

**已验证可用**

| 能力 | 验证方式 |
|---|---|
| 数据层 | `node test/smoke.mjs` — 建树 / 幂等 / 改名移位删除 / 摘要字段 / 复习视图 / 文件损坏容错 |
| 宿主半边挂载 | `node test/mount.mjs` — 模拟 cordis 真实调用 `apply()` 与每个工具的 `execute()`，含摘要管道与面板 RPC |
| 摘要管道 | `node test/summary.mjs` — 门槛 / 生成 / 落盘 / 失败重试 / 冷却 / 会话窗口过滤（假 session + 假 llm） |
| 浏览器半边渲染 | `node test/client.mjs` — 迷你 React 真渲染面板：普通会话 null / 树视图 / 复习视图 / 各种摘要状态提示 |
| **真机摘要管道** | 用真机真模型（deepseek-flash）跑修复后的 `SummaryPipeline`：`reasoningEffort=off` + `maxTokens=1200` → 一次调用拿下 2-3 句摘要 |
| 真机失败诊断 | `$DSH_HOME/learning-mode/rpc.log` 会记 `note failed ... :: 原因`；`node.noteError` 同样留档，面板 hover 可见 |
| 预设可加载 | `node scripts/validate-preset.mjs` — 32 行全部可解析（跨平台，无硬编码路径） |
| **预设真机挂载** | `ctx.agentPresets.standingKeyFor('learning-mode')` 在运行中的 DSH 里返回正常（= 组合真的挂起来了） |
| **浏览器半边被收录** | `clientModules.graph()` 里存在 `dsh-learning-mode`（61 个客户端模块中） |
| 面板 → 宿主写路径 | 上一台机器真机点击 [✓ 学会]，宿主 `rpc.log` 记到 `op=done`，树文件里该节点变 done |
| 本机安装 | profile 的 `dependencies` + `dsh.profile.bundles` 已含 `dsh-learning-mode`，node_modules junction 已建 |

**第一轮真机反馈已修（2026-09-18）**

| 现象 | 根因 | 修法 |
|---|---|---|
| 标完成后面板显示"摘要没生成出来" | 模型默认 `reasoningEffort=high`，500 token 预算全烧在思考上 → `finish = max-tokens`、文本块为空 | 显式 `reasoningEffort: 'off'` + `maxTokens: 1200`；被截断时有产出就收下；模型不认 effort 时自动去掉重试一次 |
| 面板"色彩不对"（深底深字） | 面板硬编码 `rgba(40,40,44,0.96)` + `color: inherit`，没跟应用主题 | 改用 `--dsw-alias-*` 主题变量（含兜底），浅色/深色主题都正常；顺带修好"复习"按钮换行 |

**第二轮真机反馈已改（2026-09-18，四条要求）**

| 要求 | 改动 |
|---|---|
| 学习树按钮加载延迟，必须切聊天框才出现 | 根因：面板拿到 `enabled:false` 就停轮询，而插件是 `agent/created` 之后才标 enabled。改为**自己回到节奏**的轮询（未启用 0.8s→5s，启用 3s，404 退避重试）；`test/client.mjs` 加了回归守卫 |
| 省上下文做了吗 | 之前**只做了一半**：有注入但无硬预算、子节点摘要无上限。现在加 `CONTEXT_MAX_CHARS=1200` + 五级降级阶梯（子摘要 6→3→1→0、未完成 8→4→2→0、最近完成 3→2→1→0、单条裁 120 字符、最后硬切） |
| 树要符合"当时的思想发散"，不追求体系 | 新增工具 `outline_capture`：默认挂到**当前焦点节点**下 + 焦点跟过去 + 全树同名复用；规则段新增"静默整理 / 发散优先 / 不要向用户报告"三条；注入块末尾加一句行动提示 |
| 自动化要无感、不让用户决策 | 工具回执只有 `captured: <路径>`（不灌树、不复述）；AI 不向用户报告挂载动作；`outline_capture` 一轮最多一次 |

**第三轮真机反馈已修（2026-09-18，含用户自己定位的 bug）**

| 现象 | 根因 | 修法 |
|---|---|---|
| 工具报"找不到节点"；点面板聚焦后下一轮像是没聚焦 | **标题里自带 `/`**（"插入/删除"、"时间/空间复杂度对照"、"Java：int[] / ArrayList 的取舍"）；旧 `findNode` 直接 `split('/')` 逐段匹配，这类节点**永远无法寻址** | `findNode` 改为**最长段优先 + 回溯**（"链表/插入/删除" → 链表 > "插入/删除"），保留段内空格；新增 `findNodeById` / `resolvePath`，**面板点击一律带 node.id**（彻底绕开标题歧义） |
| 手动聚焦「快慢指针」，说"介绍"，被介绍成「插入/删除是否 O(n)」 | 焦点解析失败 → 注入块里没有【当前节点摘要】，而【未完成】**是全树广度优先的前 8 条**，模型就从里面挑了一个话题 | ① 焦点修好后注入块有摘要；②【未完成】改为**只给同一层**（聚焦节点的子节点 + 兄弟），标签改成【这一层还没学完】；③ 焦点行改成 `▶ 当前聚焦（用户正在看的）`；④ 规则第 1 条 + 注入块行动提示都写明"**没指明对象时＝当前聚焦节点**" |

> 修完后同一场景的注入块只有 133 字符，且没有歧义：
> ```
> 学习树：… ｜ ▶ 当前聚焦（用户正在看的）：数据结构/链表/快慢指针
> 【进度】已完成 0/19
> 【这一层还没学完】插入/删除
> （用户没指明对象时＝上面那个"当前聚焦"；答完静默把新知识点 outline_capture 到聚焦节点下）
> ```
> 注：老会话里 sessions.json 存的旧焦点路径**不用迁移**——最长段解析会把它们重新对上。

**第四轮：交互模型定稿并落地（2026-09-18）**

用户看完《插件全貌与运行时对齐》（`设计文档/`）后确认了 7 条，据此改动：

| 用户确认 | 落地 |
|---|---|
| 聚焦是**下钻**，但允许"没学完直接切走" | 不设限制；横向切走后回原位靠"在树里点那个节点"（**不做焦点栈**，用户明确"够"） |
| **点子节点 ✓ 完成 → 自动聚焦上一层** | `completeNode()`：读窗口 → 写状态 → 触发摘要 → **再**把焦点上移到父节点；顶层/非焦点/撤销都不搬 |
| 摘要**只在点 ✓ 时**生成 | 删掉 3 条 blur 触发（面板聚焦、工具聚焦、capture 搬焦点）；现在只剩 `done` 与 `manual` 两个调用点 |
| 补"每节点最后进入时间" | `sessions.json.lastEnteredAt`（≤80 条）+ `store.windowStart()`；摘要窗口不再依赖当前焦点 |
| 开场改成"第一句问出来就出选项" | 注入块新增【现有学习树】+【开场·只做一次】（仅未用过树的会话）；选项＝推荐老树 → 其它相关树 → 新建（建议名）→ 用户自己填；`opened` 标记确认后即消失 |
| 非学习内容的确认一律用结构化提问 | 规则段新增"元决策"一节：必须 `ask_user_question` 出选项，不要对话来回问 |
| 面板操作延迟 | **乐观更新 + 操作后立刻 `tick()`**：点 ✓ 立刻划线，后台再对齐（原来要等最多 3 秒轮询） |

**第五轮真机反馈已修（2026-09-18）：子节点摘要写成了"上一层主题"**

真机现场（`AI Agent 论文` 会话，`session-6c6c5952…`）：AI 在父节点「前置知识：为什么需要模拟器」里用
`## 1./## 2.…` 一口气讲了 6 个前置概念，树里自动存成 6 个子节点；用户在**面板上直接给子节点
「rollout 的交互税」点了 ✓**（它正是当轮讲的一个概念，很简单，看一眼就懂）。
结果生成的摘要是**整篇 Dream-RSI 的核心思想**，与"rollout 的交互税"无关。三个原因叠加：

| # | 根因（都有证据） | 修法 |
|---|---|---|
| 1 | 该子节点**从没被聚焦过**（只有父节点被 `outline_focus` 过），`windowStart` 找不到 `lastEnteredAt[nodeId]` → 直接 `return 0` = **整场会话**。证据：`noteSource.from = 1789700326017` = 会话第一条消息的时间 | `windowStart` 改**三级兜底**：自己 → **最近进过的祖先** → 节点 `createdAt`（`newNode` 新字段）；0 只应出现在"什么记录都没有"时。真机验证：同一节点窗口从"整场"变成 `03:11:14.624Z`（= 父节点进入时间，精确命中） |
| 2 | 即使窗口对了，原料仍是**一条 3968 字、含 6 个小节**的 AI 消息；`clipTranscript` 又从**尾部**截，标题在第 1 节的内容反而可能被截掉 | 新增 `narrowTranscript()`：按节点标题把原料**切到命中的那一节**（`## 1. xxx` / `1. xxx` 都认，同级下一个标题结束；`#` 层级能正确嵌套）。真机：4559 字 → **426 字**（正好是第 1 节全文）。收窄后不足 120 字则退回整段，别把模型饿到只能答"无内容" |
| 3 | 提示词只给了一句 `知识点：<path>`，没告诉模型"这条消息里还有 5 个别的话题" | `summarizePrompt` 改成点名**本次要总结的知识点 + 树中位置 + 同层其它知识点（明确"不属于本次总结"）**，并加一句"原料里可能同时讲了别的知识点甚至整篇论文，那些一个字都不要写进来"；系统提示词同步强调"只写属于这一个知识点的内容" |

**真机验证（不是推断）**：用解码后的真实会话日志 + 修复后的 `SummaryPipeline` 真跑一次
（provider/model = 会话实际的 `qianwen/qwen3.8-flash`），得到 161 字摘要：

> rollout 的交互税指 RL 学习必须通过真实交互产生轨迹，而每次交互都有明确成本，在 Dream-RSI 里一次成本就是一轮 discovery-agent 调用。更关键的是层级差异：评估单个候选只需一次调用，但评估探索策略是否有效需要一整轮 110–640 次调用及真实墙钟时间，使交互税在元层被放大约两个数量级。

已直接写回 `trees/ai-agent-论文.json`（`noteSource.reason = "bugfix-repair"`），错的那条被替换掉。
回归断言加在 `test/summary.mjs`（这整段真机形状：窗口 + 收窄 + 提示词边界）与 `test/smoke.mjs`（窗口三级兜底）。

**未完成 / 待确认**

- 面板浮层的**真机可见性**（portal + 高 z-index 的改动在这一版仍未在真人浏览器里点过）——需要你刷新页面后目视确认
- 原生右侧面板页签（`sidebar.right.pane.tab` + `ctx.sidebarRightTabs` 两段式注册），替掉现在的浮层
- 间隔重复（SM-2）提醒、导出 Markdown、删除整棵树
- **摘要管道的真机效果**：单测覆盖了全部分支，但"点 ✓ → 几秒后真的出现摘要"要在真人会话里跑一次才算数
- ⚠️ **每轮注入的真机效果**（2026-09-18 补记）：见 §4 坑 13——注入在真机上**从未生效**过，重启后需要真人会话确认一次：聚焦某节点 → 说「介绍下」→ 答的就是那个节点

---

## 3. 在新机器上跑起来（4 步）

前置：DSH 已安装并能启动（`dsh web` 能跑）；Node 18+。

```bash
# 0. 进入插件目录
cd dsh-learning-mode

# 1. 建依赖 junction（把 harness 的 @deepseek-ai 包"借"给本插件）
node scripts/link-harness-deps.mjs

# 2. 自检（四套断言 + 探针，全绿再往下走）
npm test

# 3. 生成 agent 预设（复制底预设 + 追加插件行 + 逐行校验）
node install.mjs standard

# 4. 注册进 profile（dependencies + bundles + junction，幂等）
node scripts/setup-profile.mjs

# 5. 重启 dsh web（或：用 super-injector 的 dev_install_package 热装配，免重启）
#    新建会话 → 预设选「学习模式」
```

环境变量（可选，脚本会自动探测，探测不到才需要）：

| 变量 | 用途 |
|---|---|
| `DSH_HOME` | DSH 数据目录，默认 `~/.dsh` |
| `DSH_PROFILE` | profile 名，默认 `web` |
| `DSH_CHECKOUT` | dsh 包安装目录（自动探测失败时指定） |

> 本机装有 **dsh-super-injector**，最好用的开发闭环是：
> `dev_install_package <插件目录>` —— 它一次做完上面第 3、4 步的 profile 改动 + junction + 动态加载，
> **不用重启**；重启后由 `bundles` 列表正常装配，两条路径一致。
> 改完 `lib/client.js` 后刷新浏览器页面即可（客户端模块图变了，client-modules 会重新发 bundle）。

---

## 4. ★ 已经踩过的坑（都是真实踩过的，别再踩一遍）

### 坑 1：bundle patch 的行名必须是**包根**，不能写子路径

```yaml
# ✅ 对
- id: learning-mode-client-host
  name: 'dsh-learning-mode'

# ❌ 错：会被静默跳过，前端毫无反应且没有任何报错
- id: learning-mode-client-host
  name: 'dsh-learning-mode/client-host'
```

根因：`@deepseek-ai/dsh-client-modules` 的 `exactPackageSpecifier()` 对含 `/` 的说明符返回
`undefined`（官方注释："or undefined for a subpath"），该行被判为"不是客户端包"并**整个跳过**。

推论：为了让 host 平面不注册任何东西，`package.json` 的 `main` 与 `exports["."]` 指向
无害的 `lib/client-host.js`（空插件），真插件走 `./host` 子路径 + 预设的 `file:` 说明符。

### 坑 2：客户端插件必须声明 `inject: ["slots"]`

cordis 对**未声明**的服务不是返回 `undefined`，而是直接抛
`cannot get property "slots" without inject` —— 于是 `apply()` 第一行就抛，插件 fiber 变 FAILED，
前端 boot 只显示 `web boot: 1 entry did not activate / dsh-learning-mode: failed`，
**不打印原始异常**。

### 坑 3：`shell.overlay` 是 root 作用域，拿不到 sessionId

它是 frame 级 list 槽位、**没有 session 标准 props**。曾想用它做兜底，在 `useEffect` 回调里调
`props.useSessions()` 反推当前会话 —— 而 hook 不能在渲染期外调用，结果是
`slot entry crashed: Minified React error #321 (Invalid hook call)`。

**正解**：面板挂在 `conversation.session.header.actions`（session 作用域，`props.sessionId` 直接给）。

### 坑 4：预设是**完整组合**，复制底预设必须校验每个 specifier

只写自己那一行 → 学习模式会话会失去全部常规工具。
真实踩过：从旧 `router` 预设复制时带进来一行 `deepseek-ai/dsh-workflow-worker-thread`，
本机没装（官方 standard 现在用 `dsh-workflow-ptc`），结果**整个预设加载失败、会话根本建不起来**。
所以 `install.mjs` 内置了逐行解析校验，解析不了直接中止、不写出坏预设。

> 更强的校验（换机器必做一次）：在运行中的 DSH 里调
> `ctx.agentPresets.standingKeyFor('learning-mode')` —— 它真的挂一遍组合，
> 能报出"哪一行没激活 / 配置非法 / 服务注册到 root realm"。

### 坑 5：面板要 portal 到 body

直接内联渲染时，DOM 在、坐标正常、无障碍点击也能打中（Invoke 走 pattern，不看遮挡），
但屏幕上就是看不见 —— 被应用的分层/裁剪吞掉。用 `ReactDOM.createPortal(panel, document.body)`
+ 极高 z-index。

### 坑 6（换 Linux 后暴露）：**不要用正则剥 `file:///`**

```js
// ❌ Windows 上碰巧对，POSIX 上必错：开头的 "/" 被一起剥掉，变成相对路径
const p = decodeURIComponent(name.replace(/^file:\/\/\//, '').replace(/^\/([A-Za-z]:)/, '$1'))

// ✅ 唯一跨平台正确写法（顺带处理 %7E、中文等百分号编码）
import { fileURLToPath } from 'node:url'
existsSync(fileURLToPath(name))
```

真实症状：`node install.mjs` 报"组合里有 1 行无法解析"——而那一行正是**插件自己**。
（本包里的路径含 `~` 与中文，会被编码成 `%7E` / `%E6%8F%92...`，正则解完少一个前导斜杠。）

### 坑 7（换 Linux 后暴露）：脚本里不要用 `process.env.USERPROFILE`

`scripts/setup-profile.mjs` 旧版用它兜底 DSH_HOME，在 Linux 上得到空串，
拼出相对路径 `.dsh/profiles/web`，直接"找不到 profile 清单"。统一用 `harness.dshHome()`。

### 坑 8（换 Linux 后暴露）：`validate-preset.mjs` 曾硬编码 Windows 路径

旧版把 `HARNESS` / `PROFILE` 与 yaml 的 import 路径全写死成 `C:/Users/<user>/...`。
现在全部由 `scripts/harness.mjs` 推断，yaml 用 `createRequire` 从 harness 借。

### 坑 9（本版新增，设计教训）：**挂载期不要 import 可选依赖**

`lib/summary.js` 最初在顶层 `import { BlockAssembler, createUserMessage } from '@deepseek-ai/dsh-llm'`。
import 是**挂载期解析**：该包一旦改名/缺失，整个「学习模式」预设加载失败 →
**学习模式开不了会话**，而这只为了一个辅助功能。
现在改成首次真正生成摘要时 `await import(...)`，失败就用内置最小实现兜底。

**推广规则**：`lib/index.js` 的静态依赖只有 `@deepseek-ai/dsh-tools`（必需）+ node 内置模块。
其余（llm / sessions / agentDefaultModel）一律 `ctx.get(name)` 惰性取 + undefined 检查。

### 坑 10：辅助 LLM 调用**必须关思考**，且失败原因要透传到 UI

真机第一次点 [✓ 学会] 就撞上：摘要节点全部 `noteState=failed`、`noteError="模型未正常结束（max-tokens）"`。
根因是模型默认 `reasoningEffort=high`——推理 token 和输出 token **共享** `maxTokens` 预算，
500 的预算被思考吃光后文本块一个 token 都没有，`finish.kind = 'max-tokens'`。

两条通用结论：
1. 插件自己做的小任务（摘要 / 标题 / 分类）**显式关思考**（`reasoningEffort: 'off'`，
   官方 `dsh-session-title-llm` 走的是 `purpose: 'session-title'` 等价路径），预算给宽一点；
2. **失败原因必须能从 UI 看到**：`noteError` 一路透传到面板 hover，否则用户只会看到
   "没生成出来"，无从判断是没额度、模型不认参、还是内容太少。
   `$DSH_HOME/learning-mode/rpc.log` 里也有 `note failed path=... :: 原因`。

### 坑 11：portal 浮层**必须用主题 CSS 变量**，别硬编码深色

面板是 `ReactDOM.createPortal(panel, document.body)`，早先写死 `background: rgba(40,40,44,0.96)`
+ `color: "inherit"`，浅色主题下 body 的文字色是深色 → "深底深字"，用户实拍反馈色彩不对。

正确做法：用 `--dsw-alias-bg-overlay`、`--dsw-alias-label-primary/secondary`、`--dsw-alias-border-l1/l2`、
`--dsw-alias-state-success-primary` 等 token（`dsh-client-ui-theme` 把变量注入在 `:root`，
portal 子节点能继承），每个都带兜底值（变量缺失也不至于变成隐形面板）。
**不要**用 `color: inherit`——portal 的继承源是 body，跟应用主题不是一回事。

### 坑 12：改完插件代码，**host 半边要重启进程**才生效

预设是"挂载一次、多会话共享"（standing mount），模块 URL 的 ESM 缓存会一直用到进程退出：
实测改了 `lib/summary.js` 后，进程内 `import()` 同一路径**仍返回旧模块**（导出常量还是旧值）。
所以：
- 改 `lib/client.js`（浏览器半边）→ **刷新页面**即可（rev 是内容 sha1，组合时重算；若进程没重启、
  且 HMR 是关的，才需要显式调一次 `clientModules.rebuilt('<包名>')` 再刷新）；
- 改 `lib/index.js` / `store.js` / `summary.js`（host 半边）→ **必须重启 dsh web**。
  这一点在交接时最容易被忽略：代码明明改了，真机行为还是旧的。
  （本机实测过：一次 bundle 热重载调用让 `dsh web` 进程重启了 —— 结果等价于"重启生效"，
  所以别把"进程重启时间晚于文件 mtime"当成"我的改动没生效"。）

---

**第九轮：从树跳回「讲它的那次对话」（2026-09-18）**

用户点名的第 2 项（跳转聊天记录）落地。**先把前端 bundle 翻完再写代码**：

| 事实 | 结论 |
|---|---|
| 会话流有没有虚拟化 | **没有**：每个已加载的轮就是一个 DOM 行，带 `data-chat-turn`；更早的分页靠 `hasMore` / `loadThrough(seq)` |
| 有没有"跳到某条消息"的官方通道 | **没有**：客户端服务只有 `sessions.open/binding/fork/search`；外壳内部有 TurnNavigator（按轮跳，含未加载的），但没暴露成服务/槽位 |
| messageId 能不能当锚点 | **不能**：全 bundle 没有 `data-message-id`；`conversation.chat.assistant-actions` 挂的是 turn tail（每轮一个），不是每条消息 |

落地（细节见坑 14/15）：

- **数据**：`node.origins = [{sid, turn, seq, time, why}]`，写在 `outline_capture` / 标学会 / `⟳` 重写摘要
  三处（都是"你此刻正在讨论它"的时刻），按 `sid+turn` 去重、最多 3 条、最新在前；快照带 `origin` + `originCount`。
- **面板**：树行多一个淡色 **[↩]**（**只有记到来源的节点才有**）；点击 = `sessions.open`（跨会话时）
  → 轮询 `[data-chat-turn=N]` → 600ms 还没出现就用 `loadThrough(seq)` 补页 → 算 `scrollTop` 滚到视口中间
  → `.lm-flash` 闪 1.8 秒后自动摘掉。
- **判据**：新增 `op=diag`；前端一次性上报真实环境（`turnRows/turnMin/turnMax/scroll/loadThrough/sessions/binding/hasMore`），
  跳转每一步记 `jump <verdict>`；全部落 `rpc.log`。
- **回填**：`npm run backfill:origins`（默认 dry-run，`--write` 才写盘并自动备份树文件）。
  它扫会话日志里的 `outline_capture` / `outline_add` 调用（这些事件自带 `turn/seq/time`），按标题对回节点。
  本机结果：**150 个节点回填 135 个**（java 87/87、数据结构 26/26、AI Agent 论文 22/22；
  剩下 15 个是内置教程树 —— 它由代码生成，本来就没有对话来源，**不显示 ↩ 是正确的**）。
- **断言**：274 → **320**（store 的来源写入/去重/上限/非法坐标；mount 端到端"倒扫事件日志算坐标" +
  没有 `eventAt` 时留 `origin diag`；client 的 ↩ 渲染条件 + 同会话/跨会话/会话没了/分页补页/无服务五条降级分支）。

---

**第十轮：来源语义补全（"对话2 聚焦了 a，a 该改指对话2"）+ 面板窗口可拖动可缩放（2026-09-18）**

**① 来源语义**。用户问："对话1 建了 a/b/c/d（都指对话1），对话2 聚焦了 a，a 的跳转应该改成对话2 吧？"
—— 原实现**不会**：来源只写在 capture / 标学会 / ⟳ 三处。

用户给的方向对，但**不能写在"聚焦那一刻"**（面板点选 + `outline_focus` 两条路都不行）：

```
对话2 · 第 5 轮：在聊 Y（日志里最后一个 turn = 5）
    ↓ 用户在面板点 a（还没提问，日志里没有新事件）
  此刻"当前轮"仍是 5 → 记成第 5 轮   ❌ 那一轮聊的是 Y
    ↓ 用户接着问"讲讲 a" → 第 6 轮才真正讲 a
```

**做法**：挂 `agent/turn-stopping`（一轮即将结束，payload 直接给 `agent` + `turn`），
在每轮结束时把**当前聚焦的节点**记成本轮来源（`why: 'turn'`；seq 取 `session.seq`）。
好处三条：① 记的是**这一轮**（不早不晚）；② 不依赖模型是否老实调 `outline_focus`；
③ 一轮最多记一个节点 —— 纯浏览点击不会污染一片。
**已知代价**（写进代码注释了）：一轮里若聊的是别的事，聚焦节点也会被记上这一轮；
下一次真正讨论会覆盖，且列表"最新在前、最多 3 条"。

**② 面板窗口**。诉求：浮层会挡文字 → 要能拖走；能拖就顺便能调大小；还要记住上次的样子。
落地：头部拖动（`cursor:grab`；按在按钮/下拉框上不算拖动）、右/下边缘 + 右下角三个手柄
（分别只改宽 / 只改高 / 改宽高）、几何存 `localStorage("lm.panel.geom")`、
**读取时按视口钳制**（换显示器/缩窗口不会跑到屏外）、动过之后头部才出现 **⟲** 恢复默认。

两个实现上的坑，记下来：

- **基准矩形必须从 DOM 量**（`getBoundingClientRect`）：默认态是"贴右 16px + 自适应高"，
  根本没有 left/top 可用；拖过一次之后才变成显式 left/top/w/h。
- **落盘只放在 mouseup**：mousemove 里写 localStorage 会卡；而 setState 是异步的，
  闭包里读不到最新值 → 用一个模块级 `geomMirror` 做同步镜像。另外要区分"拖了"和"只点了一下"，
  否则点一下头部就会把当前矩形固化成用户设定、平白多出 ⟲。
- 拖动/缩放**不用 `useEffect`**（离线测试靠 `effects[0]` 驱动轮询，多插一个 effect 会打乱顺序），
  而是 mousedown 时把 move/up 挂 `window`、松手立刻卸掉。

- **断言**：320 → **353**（host：注册 turn-stopping + 记到第 9 轮 + why=turn + 没聚焦的节点不受影响 +
  焦点为空时不写；client：默认态不变 / 读回上次几何 / 钳制 / 拖动 / 三种缩放 / 不落盘 / ⟲ 恢复）。

---

**第八轮：面板玻璃化 + 入口按钮重做 + 树变更提示（2026-09-18）**

用户点了三件事（1 液态玻璃可行性 / 3 按钮太单薄 / 4 树变更要在按钮上提示），2（跳回聊天记录）先记账后做。

| 项 | 结论/落地 |
|---|---|
| 液态玻璃 | **真折射做不到**：`backdrop-filter` 只吃 `blur/saturate/...`，不接受 `url(#svgFilter)`，`feDisplacementMap` 那套在浏览器里实现不了。做的是 material 质感：`color-mix(...bg-overlay 74%, transparent)` + `backdrop-filter: blur(16px) saturate(1.7)` + 内高光 + 外阴影 + 悬停流光 + `@supports` 降级。**配方抄的是 DSH 自己的浮层**（打包 CSS：`color-mix(...bg-base 72%, transparent)` + `blur(6px)`），所以跟原生浮层是一家 |
| 入口按钮 | 从"1px 边框扁按钮"改成**玻璃药丸**：`◆ 学习树 3/22`（内联 SVG 树图标 + 进度数字），悬停上浮，有变更时挂红点 `+N` |
| 树变更提示 | 节点已有 `createdAt`；会话绑定新增**水位线 `seenAt`**（`markEnabled`/`autoBind`/`switchTree` 都种/重置）。比它新的节点 = 新增 → 按钮红点 `+N`、树里 `NEW` 角标、**强制展开祖先分支**、顶部一行"上次看过后新增 N 个知识点"。点开面板即清红点（乐观 + `op=seen` 落盘），`NEW` 保留到你关窗；**关掉再开自然消失**（算出来的，不是存出来的）。删除不标记（只认 `createdAt`） |

新增 RPC `op=seen`；`state` 多回 `seenAt`；`snapshotNodes` 多回 `createdAt`。
客户端断言：玻璃类名、红点 `+N`、点开清红点 + 发 `seen`、面板里 `NEW`、折叠父分支被强制展开、
水位线推进后无 `NEW`；宿主断言：`seenAt` 种下/推进/换树重置/未绑定返回 null、快照带 `createdAt`。

**第七轮：给学习会话减负（用户："学习模式不需要 superinject 注入，属于污染上下文"）**

学习会话实测每轮白带的开发向上下文，两处：

| 项 | 量 | 处理 |
|---|---|---|
| super-injector 的引导段（"本环境装有 dsh-super-injector（dev_* 工具）…"，它自己注册的 `systemPrompt.context`） | ~700 字符/轮 | 改**注入器**：`systemPrompt.context` 的文本改成按会话的 agent 预设条件注入，静音预设默认 `['learning-mode']`（可配 `mutePresets`）。判定优先读 `sessionProjections.stateOf(session,'agentPreset')`（实测：本机 dev 会话 = `cordis`，学习会话 = `learning-mode`），事件 `agent-preset/selected` 作兜底。文本仍是静态常量，只是"给不给"按会话决定 |
| 18 个 `dev_*` 工具 schema | **5567 字符（62 个工具的 14.5%，约 1.4k token）/轮** | 改**学习插件**：`agent/created` 时在该 agent 作用域里 `tools.restrict({ deny })`，`deny` 从 `tools.schemas()` 里按前缀 `dev_` 现算（不硬编码工具名，注入器不在时自动为空）。全局注册不受影响——cordis 预设里照常能用注入器 |

两处都**不静默**：注入器静音是显式按预设；学习插件的 restrict 结果与失败原因都记进 `rpc.log`
（`inject diag: deny-dev-tools:18` / `deny-dev-tools-failed:<msg>`）。

配套新增 `scripts/verify-live.mjs`（`npm run verify:live [sessionId前缀]`）：直接解**真实会话日志**
（多帧 zstd，按 magic `28 B5 2F FD` 逐帧解压）核对四件事——① 每轮注入是否真的出现在运行时快照里
（`▶ 当前聚焦`/`【进度】`）；② 学习会话是否还有 super-injector 引导；③ `dev_*` 工具是否已屏蔽；
④ `rpc.log` 最近有没有注入诊断。**这是"真机到底生效没有"的判据工具**，重启后跑一次就知道，
不用再靠"看起来好像行"。

**第六轮真机反馈（2026-09-18，紧跟注入修复）：`outline_capture` 不该搬焦点**

用户原话："为什么介绍完「World Model 与 Dreamer 的 dreaming」就开始介绍「RSSM：确定性记忆与随机性潜变量」了？
如果有子知识点，创建并简单介绍是对的，但为什么聚焦介绍了？"

日志证据（`session-6c6c5952`，注入修好后第一轮真机）：

```
04:38:13 面板点「World Model 与 Dreamer 的 dreaming」→ 焦点在这
04:38:18 用户："介绍下"                      ← 注入块正确（▶ 当前聚焦 = World Model…）
04:39:36 AI 写出该节点的完整讲解
04:39:36 AI 调 outline_capture({title:"RSSM：确定性记忆与随机性潜变量"})
         ↳ 捕获同时把焦点搬到了 RSSM（设计如此）
04:39:36 运行时快照变化 → 注入块变成"▶ 当前聚焦 = RSSM…"
04:40:58 AI 顺着新焦点又写了一整节 "# RSSM：…"（4279 字，5 个小节）
```

**根因是一个反馈环**：`outline_capture` 搬焦点 → 注入快照变化 → 模型被告知"用户现在在看 RSSM" →
它就接着讲 RSSM。注意这个环**只有注入修好之后才可能出现**：以前模型根本收不到"焦点变了"，
所以坑 13 修完立刻把这条设计缺陷暴露出来了。

**修法：让焦点只跟着用户走**

- `store.capture()` 的 `focusIt` 默认从 true 改成 **false**（只有显式 `true` 才搬）；工具
  `outline_capture` 的 `focus` 参数默认 **false**。
- 规则段改写：`outline_capture` 不搬焦点；**焦点什么时候该动**只有两种——用户的话指向某个已有节点
  （先 `outline_focus`）／用户这一问的主题就是一个新知识点（`outline_capture({focus:true})`）。
- 新增一条：**子概念只点到为止**——顺带引入的子概念挂上去 + 一两句交代，不要在同一个回答里替用户
  展开成完整的一节（想深入他自己点进去）。
- 注入块末尾的行动提示改为"…outline_capture 到聚焦节点下，**不要搬焦点**——焦点只跟着用户走"。
- 断言：`capture 默认不搬焦点` / `焦点没动 → 下一个知识点仍挂在同一层` / `capture 显式 focus=true 才搬焦点`
  / `复用已存在节点也不搬焦点`（smoke + mount 两层）。旧断言里"capture 后焦点跟到新节点"按新语义改掉了。

### 坑 13（最严重的一个）：动态状态段**从来没有真正注入过** —— `ctx.agents` + 裸 catch

**症状**：用户"手动聚焦某个节点 → 说『介绍下』"，AI 讲的是别的东西（比如父节点主题）。
用户的原话是"**ai 没有被聚焦的上下文注入**"——他说对了，而且不是"这次没有"，是**一直都没有**。

**根因**（两条叠加，第二条才致命）：

```js
text: () => {
  try {
    if (!ready) return ''
    const agent = ctx.agents.currentInitiator()   // ← ① 没写进 inject
    ...
  } catch { return '' }                            // ← ② 把致命错误吞成了"空注入"
}
```

1. 插件 `inject = ['tools', 'systemPrompt']`，**没有 `agents`**。真实 Cordis 代理对未声明的服务属性会抛
   `cannot get property "agents" without inject`（本机实测）。正确写法是官方 `dsh-sandbox-policy` /
   `dsh-user-approval` 那样，从**装配上下文参数**里取：`text: (context) => { const agent = context.agent; ... }`
   —— 框架每次 `assemble` 都传 `assembleContextFor(agent)` = `{ agent, scope, signal }`。
2. 裸 `catch { return '' }` 让"每轮自动注入焦点/摘要/待办"这个**核心功能静默失效**：没有日志、
   界面上也看不出异常。所以前几轮为"答错对象"做的修复（`findNode` 认含斜杠标题、
   【这一层还没学完】改成同层、【没指明对象＝当前聚焦】提示行）**在真机上全都没有机会生效**——
   它们改的是 `contextText()` 的输出，而那段文本压根没送到模型。

**判定证据**（可复现，不是推断）：

```bash
# ① 真实运行时里 ctx.agents 抛错，而 ctx.get('agents') 正常
#    （dev_stage_add 探测：注入列表实为 loader,timer,tools,systemPrompt,webServer）
ctx.agents                                      → THROW: cannot get property "agents" without inject
ctx.get('agents').currentInitiator().session.id  → session-286a6fa0-…（正常）

# ② 扫全部会话日志：只有"我自己的开发会话"含注入块标记（因为我原文引用过它），
#    4 个学习模式会话（6c6c5952 / d6a177ca / 3ab02a4a / 7cb9f620）**一个都没有**
#    （session.v3.jsonl.zstd 是多帧 zstd，按 magic 28 B5 2F FD 逐帧解压后搜）
#    '▶ 当前聚焦' / '【进度】' / '【这一层还没学完（' 命中数 = 0
```

**修法**：

- `text: (context) => context.agent?.session?.id`（契约优先）；仅当装配上下文缺 agent 时才退到
  `ctx.get('agents')`（**不是** `ctx.agents`）兜底，并记一条 `agent-from-registry-fallback`。
- 空注入/异常**不再静默**：按原因各记一次 `$DSH_HOME/learning-mode/rpc.log`
  （`inject diag: not-ready` / `no-agent` / `no-session-id` / `empty-block:<sid>` / `throw:<msg>`），
  `agent/created` 绑定失败也留痕（`bind-failed:<msg>`）。
- **测试桩改成忠实的**：`test/mount.mjs` 原来把 `agents` 当普通属性塞进假 ctx，比真实运行时宽松，
  正是它让 235 条断言全绿而功能全死。现在假 ctx 换成 **Proxy**：未声明属性一律抛
  `cannot get property "x" without inject`；并新增"按框架契约调 `contexts[0].text({ agent })`、
  断言注入正文非空且含【进度】"的断言。**验证过它能抓住老 bug**：把老那行改回去，7 条断言立刻红。

**教训**：① 只用框架保证的契约（官方插件怎么写就怎么写）；② 面向模型的功能，任何时候都不许把
异常吞成"空输出"——空输出和"功能没工作"在界面上长得一模一样。

### 坑 14：「跳到那次对话」只能按 **轮（turn）** 定位 —— 没有官方通道

需求是"从树上某个知识点跳回讲它的聊天记录"。把前端 bundle 翻完之后的事实：

- 客户端服务里**没有**"跳到某条消息"的接口（只有 `sessions.open / binding / fork / search`）；
- 外壳自己**有**这个能力（右侧 TurnNavigator 点一下就能跳到任意轮，未加载的先 `loadThrough(seq)` 补页），
  但它是 `ChatView` 内部回调，**没有对外暴露**成服务或槽位 prop；
- 会话流**没有虚拟化**：每个已加载的轮就是一个 DOM 行，带 `data-chat-turn` / `data-chat-anchor-key`；
  更早的分页挂在 `hasMore` / `loadOlder` / `loadThrough(seq)` 上；
- **messageId 在聊天 DOM 里没有任何锚点**（全 bundle 搜不到 `data-message-id`），
  而 `conversation.chat.assistant-actions` 其实是挂在 **turn tail** 上（每轮一个），不是"每条消息"。

**结论 / 做法**：坐标记 `(sid, turn, seq)`，跳转 = `sessions.open` → 轮询 `[data-chat-turn="N"]`
→ 600ms 还没出现就用官方 `loadThrough(seq)` 补页 → 找到后**自己算 `scrollTop`** 写进
`[data-conversation-scroll]`（**不用 `scrollIntoView`**：外壳自己也在记账滚动位置/follow-end，
`scrollIntoView` 会连带滚祖先并和它打架；它内部也是算 `flowTop` 再写 `scrollTop`）。
唯一依赖的非公开契约就是那两个 `data-*` 属性 —— 所以**降级链必须完整**：找不到行就
"会话照样切过去 + 给一句提示"，并把 `jump <verdict>` 写进 `rpc.log`。

**顺带**：这个功能让 `outline_capture` 的 `focus` 默认值又暴露了一次历史包袱 —— 记来源时不要动焦点。

### 坑 15：前端的事实必须能落盘 —— 加一条 `diag` 通道

这个项目栽过两次"单测全绿、真机是死的"，两次都是**前端/契约层**的问题，而**前端出问题只能看
console，console 不落盘**（`scripts/diag-console.mjs` 也只能实时抓）。

**做法**：host 加一个 `op=diag`（把 `payload.line` 写进 `rpc.log`），浏览器半边在**第一次拿到 state
且聊天区已渲染出至少一行**时上报一次真实环境：

```
diag: env {"session":"session-…","scroll":true,"turnRows":12,"turnMin":1,"turnMax":6,
           "loadThrough":"function","sessions":"function","binding":"ok","hasMore":false}
```

跳转的每一步也走同一条通道（`jump ok / ok-after-switch / no-row / switched-no-row / session-gone /
no-sessions / bad-origin`）。**这就是"跳转到底成没成"的离线判据** —— 不用再靠用户描述现象猜。
探测本身也踩了一个小坑：额外 `setTimeout` 会污染 `test/client.mjs` 对轮询节奏的断言
（那里是数 timers 的），所以兜底计时改成**复用轮询自己的 tick**（记 `envFirstAt`，超过 10 秒强报一次）。

### 坑 16：`dev_reload_package` 对本插件会**卡死**（其实它把整个进程重启了）

现象：对本插件调 `dev_reload_package`，工具调用会"卡很久"，最后以
**`The tool call was interrupted ... outcome is unknown`** 结束（结果永远拿不到）。

根因（有实证）：本插件是 **profile bundle 装载**的（profile `package.json` 的 `dependencies` + `bundles`
+ node_modules junction），**不是注入器注入的** —— 注入器注册表里查不到它：

```
~/.dsh/super-injector/reload-debug.log
[2026-09-18T06:58:16.507Z] reload match=learning-mode fullName=learning-mode
  activeEntry=none fiberState=? entry.disabled=? options.disabled=?
```

`activeEntry=none` → 落到"卸不掉就自杀 + 排程重启器"那条路 → **进程被整个重启** →
发起调用的工具结果随进程一起消失。所以这个"卡死"是**必然**的，不是偶发。

**判据（确认它到底成没成）**：① `ps -eo pid,lstart,cmd | grep "dsh web"` 看进程启动时间；
② 与 `ls -la --time-style=+%H:%M:%S lib/*.js` 的文件 mtime 比 —— **启动时间晚于 mtime = 新代码已生效**；
③ `tail ~/.dsh/super-injector/reload-debug.log` 看那行 `activeEntry=`。

**做法**：改 host 半边就老实重启（坑 12），别指望 `dev_reload_package`；
它对本插件唯一的"效果"就是硬重启一次。要真正热重载，得用 `dev_inject_plugin` 注入的那种形态。
**顺带**：浏览器半边的 rev 是**内容 sha1、组合时算**的，所以进程一重启，`lib/client.js` 的新内容就自动
带上新 rev —— 不需要单独调 `clientModules.rebuilt()`，**只要刷新页面**。

### 坑 17：`test/mount.mjs` 的假会话时间戳写死了 → 过了那一刻每次跑都挂（时间炸弹）

现象：`npm test` 在 `mount.mjs` 崩掉，而且**每次必挂、不是偶发**：

```
  FAIL 插件自己调了模型（不进聊天记录） :: 0
TypeError: Cannot read properties of undefined (reading 'messages')
    at test/mount.mjs:209
```

（`llmCalls` 是空的 → 下一行取 `llmCalls[llmCalls.length - 1].messages` 直接炸。）

根因：假会话的事件时间写死成 `Date.parse('2026-09-18T10:00:00.000Z')`，而**摘要窗口的起点用的是真实
`Date.now()`** —— `store.windowStart()` 三级兜底（自己 `lastEnteredAt` → 最近进过的祖先 → 节点
`createdAt`）全是真实时钟，`extractTranscript` 又会整条丢掉 `time < floor` 的事件。
于是系统时间一走过 `2026-09-18T10:00:04Z`（最后一个假事件），**全部假事件都落到窗口之前** →
`meetsThreshold` 不过（0 条 / 0 字）→ 走 `too-little` 跳过分支 → 插件根本没调模型 → 断言必挂。
不是功能回归：把时钟拨回 `09:00Z` 再跑**同一份 `mount.mjs` 就是 ALL PASS**（2026-09-18 实测）。

修法：假事件时间改成**相对 now 之后** —— `const t0 = Date.now() + 1000`。
测试语义本来就要求"这段对话发生在节点被进入之后"，而节点是测试运行期建的
（`createdAt`/`lastEnteredAt` 都是此刻），所以假事件必须晚于此刻；写死一个绝对时刻只是碰巧曾经成立。

**通用判据**：凡是被测代码用真实时钟做窗口/冷却（`Date.now()`、`COOLDOWN_MS`、`lastEnteredAt`），
测试里的假数据时间**必须相对 now 生成**，绝不能写死绝对时间戳 —— 否则就是一颗定时炸弹，
爆炸时刻还取决于无关的时区/日期，极容易被误判成功能回归。

### 坑 18：`--dsw-alias-brand-primary` 是**中性 brand**，"brand 底 + 白字"在暗色下是白底白字

现象（暗色模式真机实拍）：树里新增节点那一行的 **NEW 角标**渲染成一块**没有文字的纯白色药丸**。

根因：角标样式写的是 `background: V.brand` + **硬编码 `color: "#fff"`**，而
`--dsw-alias-brand-primary` 并不是"品牌蓝"，它是这套设计系统的**中性 brand**：

| 主题 | 选择器 | `--dsw-alias-brand-primary` | 解析值 |
|---|---|---|---|
| 浅色 | `body` | `--dsw-static-neutral-bluish-1000` | `#0f1115`（近黑） |
| 暗色 | `body[data-ds-dark-theme]` | `--dsw-static-neutral-bluish-50` | `#f9fafb`（近白） |

所以浅色下是"近黑底 + 白字"（正常），暗色下是"近白底 + 白字"= **白底白字**，文字直接消失。

修法：用设计系统自己的配对前景色。`--dsw-alias-button-primary-fill` 的值正是
`var(--dsw-alias-brand-primary)`，而它的配对文字色是 `--dsw-alias-label-primary-foreground`
（浅色 `#fff` / 暗色 `#0f1115`）。于是 `V` 里补一个 `brandFg`，角标改成
`background: V.brand, color: V.brandFg` —— 两个主题下都读得出来。

**为什么单测没拦住**：原来只有一条纯文本断言 `marked.includes('NEW')` —— 白底白字
同样是"渲染出了 NEW 文字"，照样通过。补的守卫必须断言**样式**：底色引用
`--dsw-alias-brand-primary`、文字色引用 `--dsw-alias-label-primary-foreground`
（且必须是 `var(...)` 形式，不能是硬编码色值）。
**已做反向验证**：把新测试放到未修复的 `client.js` 上跑，精确报出
`FAIL NEW 角标文字色用主题配对前景色，不硬编码 #fff :: "#fff"`。

**通用判据**：凡是用 `--dsw-alias-*` 当**背景**，前景色就必须同时取设计系统的**配对 token**
（`label-primary-foreground` / `label-primary-inverted` 等），**绝不能硬编码 `#fff` / `#000`**
—— 这套 token 在暗色下会整体反转，硬编码一定会有一个主题下瞎掉。
另外：断言"某样东西渲染出来了"，光看文本不够，**要看样式**。

### 坑 19：i18n 引入的标识符 `t` 会被局部 `t` 遮蔽（地雷，不是当时的 bug）

现象：给宿主侧文案接 i18n 时，`store.js` / `summary.js` / `index.js` 里本来就有
`const t = Date.parse(...)`、`.map((t) => …)`、`.find((t) => …)` 这类局部 `t`。
JS 的遮蔽**不报错**：在那些作用域里写 `t('中文')` 会变成"调用一个数字/对象" → 运行时 TypeError。

要说清楚的是：**这本来不是 bug**。遮蔽是作用域局部的 —— 若 `t('…')` 调用在函数体里、
而 `(t)` 只是该函数内某个箭头的参数，箭头的参数只遮蔽箭头体内部，调用照样解析到 import。
所以这是**拆地雷**：防的是以后有人往那些作用域里加一句 `t('…')` 时静默变 TypeError。

做法：把 `const t = …` / `(t) => …` 改名（`t` → `heading` / `item` / `tree`），
让 `t` 这个标识符在文件里**只可能是 import 进来的翻译函数**。
自查判据（正则扫全文）：`(const|let|var)\s+t\s*=`、`\(\s*t\s*[,)=]`、`\bt\s*=>` 全部为 0。

**顺带踩到的第二个（这个是真 bug）**：浏览器半边没接上 locale 服务时的**兜底翻译函数**
只写了 `return key`，于是 `T('已完成 {done} / {total}', {...})` 把占位符**原样漏到界面上**
（真机表现："已完成 {done}/{total}"）。兜底也必须做插值，规则与 `dsh-client-locale` 一致。
这是 `test/client.mjs` 的中文断言抓出来的 —— 又一次印证"中文路径的回归断言不能省"。

---

## 5. 目录结构

```
dsh-learning-mode/
├── package.json            main/exports["."] -> 空插件 client-host；./host -> 真插件；./client -> 浏览器半边
├── install.mjs             生成 agent 预设（复制底预设 + 追加行 + 校验）
├── cordis.patch.yml        bundle patch：只插空插件（让本包被 client-modules 扫到）
├── lib/
│   ├── store.js            数据层：多棵学习树 + 会话绑定 + 摘要字段 + 复习视图（纯 Node，可单测）
│   ├── summary.js          摘要管道：门槛 / 会话窗口抽取 / ctx.llm 调用 / 失败重试（惰性 import）
│   ├── index.js            宿主半边：8 个 outline_* 工具 + 规则段 + 动态状态段 + 摘要触发 + 面板 RPC
│   ├── client.js           浏览器半边：会话头部「学习树」按钮 + 浮层面板（树视图 / 复习视图）
│   └── client-host.js      空插件（host 平面挂它，什么都不注册）
├── scripts/
│   ├── harness.mjs         跨机器发现 DSH 安装位置
│   ├── link-harness-deps.mjs  建依赖 junction
│   ├── setup-profile.mjs   注册进 profile（dependencies + bundles + junction）
│   ├── verify-live.mjs     真机核查（解多帧 zstd 会话日志：注入在不在、dev_* 有没有被屏蔽）
│   ├── backfill-origins.mjs 回填来源坐标（扫日志里的 capture/add 调用，按标题对回节点；--write 才写盘）
│   └── validate-preset.mjs 逐行校验预设 row 的 name 能否解析
└── test/
    ├── smoke.mjs           数据层断言
    ├── mount.mjs           宿主半边集成断言（含摘要管道 + 面板 RPC）
    ├── summary.mjs         摘要管道分支断言（假 session / 假 llm）
    ├── client.mjs          浏览器半边渲染断言（迷你 React）
    └── probe.mjs           最小挂载探针
```

工具面（9 个）：`outline_projects / open / show / add / update / done / focus / review / capture`。
`outline_capture` 是"无感整理"的核心：就地挂 + 焦点跟随 + 同名复用，回执极简。
面板 RPC（`POST /api/learning-mode/rpc`）：`state / review / open / create / focus / done / note`。

---

## 6. 数据与卸载

- 数据：`$DSH_HOME/learning-mode/index.json`（树清单）、`trees/<id>.json`（每棵树）、
  `sessions.json`（会话绑定）、`rpc.log`（面板调用 + 摘要管道日志，诊断用）
- 节点新增字段：`noteState`（running/done/skipped/failed）、`noteTries`、`noteError`、`noteSource`
  —— 旧数据没有这些字段也能读（按空值处理，无需迁移）
- 卸载：删 `$DSH_HOME/.agent-presets/learning-mode/`；profile 的 `dependencies` 与
  `dsh.profile.bundles` 里去掉 `dsh-learning-mode`；删插件目录
- 数据备份（可选）：把 `数据备份（可选）/` 里的内容拷到新机器的 `$DSH_HOME/learning-mode/`
  （只会恢复树，不恢复会话绑定 —— 会话 id 换机器就没意义了）

---

## 7. 下一步建议

1. **真人验一遍**：刷新页面 → 新建「学习模式」会话 → 点几个 [✓ 学会]
   - 面板是否可见（portal 改动）
   - 点 ✓ 后几秒内是否出现摘要（`$DSH_HOME/learning-mode/rpc.log` 会记 `note ok ...`）
   - [复习] 页签是否列出已完成 + 摘要
2. **原生右侧面板页签**：`ctx.sidebarRightTabs.register({ id, kind, title })` 两段式 +
   `slots.register({ name:'sidebar.right.pane.tab', key:<id> }, Body)`，参考官方
   `dsh-client-ui-sidebar-files/lib/client.js`（本机已装，可当范本）
3. 复习视图增强：只列"摘要待补"、按时间分组、导出 Markdown
4. 间隔重复（SM-2）：`doneAt` / `noteAt` 已有，加 `reviewAt` + 到期提醒即可起步
5. 面板右键/悬停菜单：删除节点、重命名（目前只能靠跟 AI 说 → `outline_update`）
6. **capture 漏调的兜底**（当前最大风险）：capture 靠提示词驱动，模型漏调 = 静默失败。
   兜底方案：监听 `agent/turn-stopping`（serial，回合将闭时触发）+ `session/event` 观察本轮
   是否出现过 `tool/call: outline_capture`；没有就用一次轻量 LLM 调用抽取知识点并静默挂载。
   代价是每轮多一次模型调用，所以先看实测漏调率再决定是否上。
7. 教程树文案已更新（新增"② 树顺着你的思路长"）；**老会话里的教程树还是旧文案**，
   想换需要手动删掉 `trees/tutorial.json` 重新生成。

---

## 8. 与其它 agent 交接的提示

诊断顺序（血泪总结）：

1. 先看 `$DSH_HOME/learning-mode/rpc.log` 有没有内容 —— 有 = 浏览器半边跑起来了
2. 没有 → 检查宿主组合：`dsh --profile web --dump-config | grep learning-mode`
3. 有组合但前端没反应 → 前端 boot 只说 "1 entry did not activate"，**不打印原始异常**；
   用浏览器控制台看 `[learning-mode]` 前缀日志，或掏 `web boot` 的原始异常
4. 预设本身是否可挂 → 在运行中的 DSH 里调 `ctx.agentPresets.standingKeyFor('learning-mode')`
   （可用 super-injector 临时挂一个 host 插件注册个探针工具，用完 `cordis_undefine`）

本包的设计文档在 `../设计文档/`（方案 v1 / v2、WeSmartFlow 调研报告），
v2 里的 §3「启动时序与就绪性」是理解"为什么挂载期不能有 I/O / 不能 import 可选依赖"的关键。

---

## 9. 多语言（i18n）：面板与模型侧文案都跟随 DSH 的语言

DSH 的语言设置决定本插件所有文案：中文（`zh`）/ English（`en`）。
**语言改了下一次调用就生效**，不需要重装或重启（工具 description 例外，见下方"两个限制"）。

### 9.1 命名空间与取值规则（照抄官方）

DSH 内置语言只有 `zh` / `en`（`dsh-client-locale` 的 `LOCALE_IDS`），且 `zh` 声明的
fallback 就是 `en`。取值链是"当前语言 → 它的 fallback → 找不到就返回 key 本身"，
插值语法是 `{name}`。本插件的实现与它逐字对齐（`lib/i18n.js` 的 `translate`）。

### 9.2 key = 中文原文（这是本项目的约定，不是笔误）

代码里写 `t('摘要生成中…')`，英文表的 key 就是这句中文：

```js
export const EN = { '摘要生成中…': 'Generating summary…' }
```

好处三条：
1. **中文路径零风险**：zh 的取值就是 key 本身，改造不会动到已经调好的中文行为/提示词；
2. **漏翻必被抓**：缺 key 时英文界面会露出中文，而 `test/i18n.mjs` 直接扫 `t('…')` 的
   第一参数并断言"每个 key 都在 EN 里"，漏翻会让测试红；
3. 新增文案只写一处（key 就是文案），不会出现"改了中文忘了英文"。

代价是代码里保留了中文串（本仓库注释本来也是中文），换来的是**结构性防漏翻**。
zh 表是对 EN 的 key 集合做**恒等映射**（`Object.fromEntries`）—— 必须显式给出，
否则 zh 会顺着 fallback 掉到英文。

### 9.3 两边各有一份字典（不能共享）

`lib/client.js` **不是 ESM**：它是 `window.__ModuleLoader__.load({ factory })` 的闭包模块，
只能 `require("react")` 这类外部依赖，**无法 import `lib/i18n.js`**。所以：

| 半边 | 字典位置 | 用谁翻译 |
|---|---|---|
| 宿主（index/store/summary/store） | `lib/i18n.js` 的 `EN` / `ZH` | `t(key, params)`（读模块级当前语言） |
| 浏览器（面板 UI） | `lib/client.js` 内嵌 `EN` + 恒等 `ZH` | `T(key, params)`（默认真值 + 插值，apply 后换成 `locale.bind(NS)`） |

两边的 key 集合基本不相交（UI 文案只由浏览器半边用，模型侧/存储侧只由 host 用）。
⚠️ 浏览器半边的翻译函数叫 **`T`** 而不是 `t` —— 那个文件里有 `trees.map(function (t) {…})`
和 `var t = window.prompt(…)`，用 `t` 会被静默遮蔽。

### 9.4 当前语言是怎么定下来的（host 侧）

优先级：**浏览器上报的 active locale** > `settings` 里显式的 `locale.preference` > 默认 `zh`。

为什么浏览器上报优先：DSH 的语言偏好存在 host settings 的 `locale` 命名空间，但
**没显式设置过时是"交给浏览器决定"的**（按 Accept-Language 排），host 根本看不到那个结果。
浏览器半边每轮 RPC 都会把真实生效的 active locale 塞进 body（`payload.locale`），
host 用 `handleRpc` 接住并 `setLocale`；每轮注入块生成前还会再同步一次。

浏览器半边侧：`apply()` 里 `ctx.locale.register(NS, 'zh'|'en', …)` 注册两本字典 +
`bind(NS)`；槽位注册项带 `locale: NS`，renderer 据此（`useLocaleRevision`）
**在切语言时自动重渲染**该 outlet —— 不用自己订阅 `locale/change`。
⚠️ 只有 locale 服务确实存在时才给注册项加 `locale`：renderer 在"声明了 locale 但没有
locale face"时会**直接抛 `SlotAssemblyError`**，那会把整个「学习树」按钮弄没。

### 9.5 两个限制（已知，接受）

1. **工具 description 在挂载时定型**：框架要求 `description` 是字符串（不支持 getter），
   `defineTool()` 在 `apply()` 里就取了值。所以 language 变了之后，注入块 / 工具回执 /
   摘要提示词立刻跟着变，而**工具描述要等进程重启**才跟着变。
2. **预设显示名没有 i18n 字段**：`preset.yml` 只支持纯文本 `name` / `description`
   （`dsh-agent-presets` 的 metadata 解析），所以新建会话时那个「学习模式」名字不随语言变。

### 9.6 相关测试

| 测试 | 覆盖 |
|---|---|
| `test/i18n.mjs` | key 齐不齐 / 英文值是不是真英文 / 占位符对齐 / 字典无重复 key / `pickLocale` + `translate` 行为 |
| `test/client.mjs` | 假 locale 服务（active 可变）：中文基线、切英文后面板与入口按钮全英文、切回中文立刻恢复 |
| `test/mount.mjs` / `test/smoke.mjs` | 默认中文路径的逐条行为（i18n 不许改动中文输出） |

`scripts/verify-live.mjs` 的注入判据**同时认中英文标记**，两种语言下都能核查真机注入。
